apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentiment-ai.fullname" . }}-service
  labels:
    {{- include "sentiment-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "sentiment-ai.selectorLabels" . | nindent 4 }}
